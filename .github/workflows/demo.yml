name: Demo workflow
on: workflow_dispatch
jobs: 
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello Github Actions!
      - name: Print to console a few lines
        run: |  
          echo First line!
          echo Second line!
